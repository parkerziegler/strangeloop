import { Image } from "spectacle";
import styled from "styled-components";

const RoundedImage = styled(Image)`
  border-radius: 10px;
  box-shadow: 10px 10px 15px -10px rgba(34, 34, 34, 0.5);
`;

export default RoundedImage;
